version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: activity_tracker
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  api:
    build: .
    environment:
      DATABASE_URL: postgresql+psycopg://postgres:postgres@db:5432/activity_tracker
    ports:
      - "8000:8000"
    depends_on:
      - db

volumes:
  pgdata:
